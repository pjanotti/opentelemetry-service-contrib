receivers:
  signalfx:
  signalfx/allsettings:

processors:
  exampleprocessor:

exporters:
  exampleexporter:

service:
  pipelines:
    metrics:
      receivers: [signal, signalfx/allsettings]
      processors: [exampleprocessor]
      exporters: [exampleexporter]
